{
  "name": "vue-lit",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild --bundle src/index.ts --format=esm --outfile=vue-lit.js --define:process.env.NODE_ENV='production'",
    "serve": "sirv --dev ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@vue/reactivity": "^3.0.0",
    "lit-html": "^1.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.7.14",
    "sirv-cli": "^1.0.6"
  }
}
